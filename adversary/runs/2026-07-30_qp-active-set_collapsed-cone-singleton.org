#+TITLE: Adversary Run: Collapsed Cone (m >> n), Feasible Set a Single Point
#+DATE: <2026-07-30>

* Problem
:PROPERTIES:
:FAMILY: qp-active-set
:CLASS: maximally degenerate; Slater fails, no interior
:SOURCE: The geometry behind pounce issue #282 (false infeasibility certificate on m/n >> 1 collapsed cones), reconstructed with a known optimum.
:KNOWN_OPTIMAL: 0 at x* = 0
:N_VARIABLES: 6
:N_CONSTRAINTS: 26 inequalities, all active at x*
:SELECTED_BY: topic-guidance (PR #412 rewrites this family)
:COVERAGE_TARGET: -
:END:

See the companion script for the exact formulation.

* Results
:PROPERTIES:
:STATUS: PASS
:END:

| Solver                   | Status   | Objective              | Rel Err    | Time (s) |
|--------------------------+----------+------------------------+------------+----------|
| pounce ipm               | optimal  | 1.438656e-09           | 1.44e-09   | 0.0019   |
| pounce active-set        | optimal  | -8.792850e-18          | 8.79e-18   | 0.0007   |
| cvxpy CLARABEL           | optimal  | 2.662550e-10           | -          | 0.0030   |

Known optimal: 0 at x* = 0

The directions positively span R^n, so the feasible set is exactly {0}: every row is active and the active set is massively rank-deficient. This is the geometry that historically produced a *false infeasibility certificate*. Neither path claims infeasibility; the active-set path returns the exact optimum (8.8e-18) and is 2.7x faster than the IPM. The probe also exercises the PR's rule that an infeasibility verdict requires a certificate.

* Cross-check / Verification
Oracle: cvxpy/CLARABEL (independent of pounce). Probe 2 adds cvxpy/SCS as a
second oracle. Both pounce engines (`method="ipm"` and `method="active-set"`)
are run on the identical problem so the new active-set path is cross-checked
against the long-standing interior-point path as well as against the oracle,
per the family's entry in the workflow table.

The safety property PR #412 is built around is asserted explicitly: a solve that
reports `optimal` with an objective off by more than tolerance is flagged
SOLVED-BUT-WRONG and fails the probe. None occurred.

* Verdict
PASS -- pounce matches the oracle and the known optimum to tolerance on both
engines, with no solved-but-wrong and no false infeasibility.
