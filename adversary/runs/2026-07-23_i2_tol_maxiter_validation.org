* [2026-07-23] i2 tol validation not propagated to minimize — SOLVER_BUG
:PROPERTIES:
:FAMILY: api
:PR_AREA: #277/#298 (tol/max_iter validation on convex entry points)
:VERDICT: SOLVER_BUG
:END:

** Problem
#277/#298 added tol/max_iter validation and explicitly lists the convex entry
points as "solve_qp, solve_socp, minimize, batch"; tol>=1.0 must be rejected
because it "accepts the non-stationary starting iterate and returns a wrong
point labeled optimal". solve_qp and solve_socp now reject every bad tol
(0, <0, inf, nan, >=1) and non-positive max_iter. `minimize` was NOT given the
guard.

** Result
solve_qp / solve_socp: all 16 bad tol/max_iter values correctly raise. PASS.

minimize on Rosenbrock (true min x=[1,1], f=0), x0=[-1.2,1]:
| call              | status | success | x               | f       |
|-------------------+--------+---------+-----------------+---------|
| minimize(tol=inf) | 0      | True    | [0.5565,0.3052] | 1.99e-1 |
| minimize(tol=1e6) | 0      | True    | [0.5565,0.3052] | 1.99e-1 |
| minimize(tol=1.0) | 0      | True    | [0.5565,0.3052] | 1.99e-1 |

minimize accepts tol>=1 and tol=inf and returns status=0 (Solve_Succeeded),
success=True at a clearly non-stationary point (f=0.199, large gradient) — the
exact failure mode #277 documents, still live on the minimize entry point.
(minimize(tol<=0 / nan) and minimize(max_iter=-5) do raise RuntimeError;
minimize(max_iter=0) returns success=False, so those are safe. The gap is
specifically tol>=1 / tol=inf being accepted with success=True.)

** Oracle
Correct expected behavior (reject the tol, or converge to the true optimum);
pounce's own default-tol solve reaches x=[1,1], f=5.6e-17, confirming the
tol>=1 point is non-optimal by pounce's own standard.

** Repro
source .venv-qa/bin/activate
python adversary/runs/2026-07-23_i2_tol_maxiter_validation.py
