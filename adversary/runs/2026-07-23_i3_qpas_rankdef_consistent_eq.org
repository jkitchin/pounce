#+TITLE: i3 — qp-active-set InternalError on exact-duplicate rank-deficient consistent equality
#+DATE: 2026-07-23

* Verdict: FAIL (SOLVER_LIMITATION) — residual gap of #313/#321/#323

Probes just outside #313/#321/#323 (which stopped the dedicated active-set QP
engine, ~pounce-qp~ via CLI ~solver_selection=qp-active-set~, from InternalError/
exit-1 on rank-deficient-but-consistent equality blocks).

All four instances constrain the SAME feasible line x+y=2 in 2 vars, so all share
the analytic optimum of min (x-1)^2+(y-2)^2 : x*=(0.5,1.5), f*=0.5.

| encoding                         | rows | pounce qp-active-set            |
|----------------------------------+------+---------------------------------|
| [x+y=2 ; x+y=2]                   |    2 | SolveSucceeded, VERIFIED  (OK)  |
| [x+y=2 ; 2x+2y=4 ; 3x+3y=6]       |    3 | SolveSucceeded, VERIFIED  (OK)  |
| [x+y=2 ; x+y=2 ; x+y=2]           |    3 | InternalError objno 500 exit 1  |
| [x+y=2 ; 3x+3y=6 ; 4x+4y=8]       |    3 | InternalError objno 500 exit 1  |

The engine solves the 2-row and distinct-scaling-trio encodings of the feasible
set but InternalErrors (objno 500, exit 1, ~pounce verify~ REJECTED) on the
exact-duplicate trio and the integer-combination trio r3=r1+r2 — different
encodings of the IDENTICAL feasible set and optimum.

Oracle: analytic KKT (unassailable) + pounce's OWN success on the r_scaled3
encoding of the same feasible set (self-consistency) + ~pounce verify~.
(ipopt structurally refuses m_eq>n with "too few degrees of freedom", so it is
not a discriminating oracle here; the analytic optimum + pounce's own scaled-trio
success carry the proof.)

Distinct from the 2026-07-22 "Active-set cycling battery (duplicated cube)"
entry, which replicated INEQUALITY facets (degenerate vertex); this is
rank-deficiency in the EQUALITY block.

Repro: python adversary/runs/2026-07-23_i3_qpas_rankdef_consistent_eq.py
